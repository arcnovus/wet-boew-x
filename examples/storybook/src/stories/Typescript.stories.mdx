import { Meta } from "@storybook/addon-docs";
import { PageTitle } from "@arcnovus/wet-boew-react";

<Meta title="Instructions/TypeScript Support" />

# TypeScript Support

`wet-boew-react` is written in TypeScript and therefore suports TypeScript out-of-the-box.
Just use the TypeScript template when bootstrapping your application and you'll
be good to go.

```bash
npx create-react-app my-app --template typescript
```
